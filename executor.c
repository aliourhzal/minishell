#include "minishell.h"

/*(void)line;
    int i = -1;
    int j;
    while(++i < main->cmds_count)
    {
        j = -1;
        while (main->full_line[i].cmd[++j])
            printf(".%s.\n", main->full_line[i].cmd[j]);
        printf("--------------------------\n");
    }*/

/*void	executor(char *line, t_minishell   *main)
{
    
}*/